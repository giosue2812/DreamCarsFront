import { MCollapsibleDirective } from './m-collapsible.directive';

describe('MCollapsibleDirective', () => {
  it('should create an instance', () => {
    const directive = new MCollapsibleDirective();
    expect(directive).toBeTruthy();
  });
});
