import { MModalDirective } from './m-modal.directive';

describe('MModalDirective', () => {
  it('should create an instance', () => {
    const directive = new MModalDirective();
    expect(directive).toBeTruthy();
  });
});
