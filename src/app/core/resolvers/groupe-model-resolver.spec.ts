import { GroupeModelResolver } from './groupe-model-resolver';

describe('GroupeModelResolver', () => {
  it('should create an instance', () => {
    expect(new GroupeModelResolver()).toBeTruthy();
  });
});
