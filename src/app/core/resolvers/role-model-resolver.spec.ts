import { RoleModelResolver } from './role-model-resolver';

describe('RoleModelResolver', () => {
  it('should create an instance', () => {
    expect(new RoleModelResolver()).toBeTruthy();
  });
});
