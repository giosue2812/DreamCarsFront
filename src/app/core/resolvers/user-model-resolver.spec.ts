import { UserModelResolver } from './user-model-resolver';

describe('UserModelResolver', () => {
  it('should create an instance', () => {
    expect(new UserModelResolver()).toBeTruthy();
  });
});
